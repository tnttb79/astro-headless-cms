# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables (Wix stores private-app OAuth credentials in .env.local)
.env
.env.production
.env.local
.env.*.local

# Google service-account key — GITIGNORED, never commit it. GitHub push
# protection blocks it, and the booking calendars hold real patient PII
# (name/phone/DOB/insurance in event descriptions), so a leaked key is a real
# risk. The key is provided out-of-band and lives only on the owner's machine /
# the deployed build. Read only in server code (src/lib/google/credentials.ts).
# Setup + risk-acceptance note: agent-context/BOOKING_CREDENTIALS.md
marin-holy-hill-*.json

# wix cli local state
.wix/

# macOS-generated files
.DS_Store

# jetbrains setting folder
.idea/
