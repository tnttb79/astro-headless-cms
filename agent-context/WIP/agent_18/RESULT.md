# What Was Done

- Restored `scroll-snap-type: y proximity` for gentle section alignment.
- Restored `scroll-snap-align: start` and `scroll-snap-stop: normal` on the existing section targets.
- Restored the reduced-motion override that disables snapping for visitors who request less motion.
- Left the homepage dividers, tonal contrast, spacing, content, and layout unchanged.
- Built and released the managed Wix frontend.

# Result

The site again provides optional proximity-assisted section alignment without mandatory snap behavior. The build completed successfully, the live homepage returned HTTP 200, and its published stylesheet contains proximity snapping, normal snap stops, the reduced-motion opt-out, and the new section-divider rule.

# Items Left to Take Care Of

None.
