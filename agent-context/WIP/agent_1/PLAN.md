# Goal

Move the two-file WIP workflow under `agent-context/` and remove the obsolete content-review document.

# Plan

1. Update the repository-wide agent rule to use `agent-context/WIP/PLAN.md` and `agent-context/WIP/RESULT.md` exclusively.
2. Remove the incorrectly placed root `WIP/` files.
3. Remove `agent-context/CONTENT_REVIEW.md`.
4. Create the current iteration result in `agent-context/WIP/RESULT.md`.
5. Verify the new WIP directory contains exactly the two required files.

