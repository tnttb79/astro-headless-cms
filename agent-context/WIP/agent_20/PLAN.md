# Goal

Right-align the mobile Menu button within the site header without changing its size, content, or behavior.

# Plan

- Preserve the brand's automatic right margin at compact widths so the Menu control is pushed to the far edge of the header.
- Leave the direct-booking visibility breakpoint and mobile navigation interaction unchanged.
- Build the Astro/Wix frontend, release it once, and verify the published responsive header CSS contains the corrected alignment.
