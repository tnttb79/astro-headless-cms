<span class="badge"><slot /></span>
<style>.badge { display: inline-flex; align-items: center; min-height: 1.7rem; padding: .18rem .55rem; border-radius: 999px; background: var(--dawn-pale); color: var(--pine-deep); font: 500 .72rem/1 var(--font-utility); letter-spacing: .04em; text-transform: uppercase; }</style>
